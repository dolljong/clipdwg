# 저장소에는 LF로 보관하고 체크아웃할 때 플랫폼에 맞춘다.
* text=auto

# PowerShell 스크립트는 UTF-8 BOM이 필요하다.
# Windows PowerShell 5.1은 BOM이 없으면 ANSI로 읽어 한글 주석이 깨지고 파서 오류가 난다.
*.ps1 text eol=crlf working-tree-encoding=UTF-8

*.sln text eol=crlf
*.csproj text eol=crlf
